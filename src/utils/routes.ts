const version = '/sims/api/v1';

export const routes = {
	auth: `${version}/auth`,
	students: `${version}`,
	classes: `${version}/classes`,
	dashboard: `${version}/dashboard`,
};

export default routes;
